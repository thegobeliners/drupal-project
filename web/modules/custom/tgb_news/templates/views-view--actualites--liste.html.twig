{% set classes = [
    dom_id ? 'js-view-dom-id-' ~ dom_id,
    'first'
] %}

<section id="section-presse-1" {{ attributes.addClass(classes) }}>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="text-center">
                    <h1>Actualités</h1>
                </div>
            </div>
        </div>

        <div class="row">{{ rows }}</div>
    </div>
</section>